
<nav>
    <% if(user) { %>
    <a href="/users/<%= user._id %>/meetings">The BallPark</a>
    <a href="/auth/sign-out">Sign Out</a>
    <% } else { %>
    <a href="/auth/sign-in">Sign In</a> 
    <a href="/auth/sign-up">Create An Account</a>
    <% } %>
  </nav>
  