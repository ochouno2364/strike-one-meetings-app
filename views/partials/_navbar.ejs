<link rel="stylesheet" href="/stylesheets/partials.css" />
<nav>
    <% if(user) { %>
    <div class="navbar">
        <div class="nav-links">
    <a class="authlink" href="/users/<%= user._id %>/meetings">The BallPark</a>
    <a class="authlink" href="/auth/sign-out">Sign Out</a>
    </div>
    </div>
    <% } else { %>
        <div class="navbar">
            <div class="nav-links">
    <a class="authlink" href="/auth/sign-in">Sign In</a> 
    <a class="authlink" href="/auth/sign-up">Create An Account</a>
     </div>
    </div>
    <% } %>
  </nav>
  