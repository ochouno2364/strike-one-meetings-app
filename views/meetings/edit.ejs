<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Edit Meeting Post</title>
  </head>
  <body>
    <%- include('../partials/_navbar.ejs') %>
    <h1>Edit Meeting Post</h1>

    <form
    action="/users/<%= user._id %>/meetings/<%= meeting._id %>?_method=PUT"
    method="POST">
        <label for="level">Coach or Player:</label>
        <select id="level" name="level" id="level" value="<%= meeting.level %>"  required>
          <option value="coach" <%= meeting.level === "coach" ? "selected" : "" %>>Coach</option>
          <option value="player" <%= meeting.level === "player" ? "selected" : "" %>>Player</option>
        </select>
        <br>
        <br>
        <label for="name">Name:</label>
        <input type="text" name="name" id="name" value="<%= meeting.name %>" required />
        <br>
        <br>
        <label for="position">Position:</label>
        <input type="text" name="position" id="position" value="<%= meeting.position %>"required />
        <br>
        <br>
        <label for="age">How old are you?:</label>
        <input type="text" name="age" id="age" value="<%= meeting.age %>" required/>
        <br>
        <br>
        <label for="school">What School do you Attend/Coach?:</label>
        <input type="text" name="school" id="school" value="<%= meeting.school %>"/>
        <br>
        <br>
        <label for="residence">What area are you located:</label>
        <input type="text" name="residence" id="residence" value="<%= meeting.residence %>" required/>
        <br>
        <br>
        <label for="information">Contact Info:</label>
        <input type="text" name="information" id="information" value="<%= meeting.information %>"/>
        <br>
        <br>
        <label for="status">Status:</label>
        <select id="status" name="status">
          <option value="Training" <%= meeting.status === "Training" ? "selected" : "" %>>Interested In Training</option>
          <option value="Looking" <%+ meeting.status === "Looking" ? "selected" : "" %>>Looking For Team</option>
          <option value="Fun" <%= meeting.status === "Fun" ? "selected" : "" %>>Just For Fun</option>
        </select>
        <br>
        <br>
        <button type="submit">Update Post</button>
      </form>
  </body>
</html>